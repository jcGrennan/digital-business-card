// importing the app and the stylesheet
import ReactDOM from 'react-dom/client';
import App from './App'
import "./styles.css"

// rendering the app using ReactDOM
const root = ReactDOM.createRoot(document.getElementById('root'))
root.render(<App />)